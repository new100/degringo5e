<fieldset class="card items">
    <legend>{{ localize "DEGRINGO5E.ADVANCEMENT.Migration.Selection" }}</legend>
    <div class="form-group hint">
        <p class="hint">{{ localize "DEGRINGO5E.ADVANCEMENT.Migration.Hint" name=item.name }}</p>
    </div>
    <div class="header">
        <div class="item-name">{{ localize "DEGRINGO5E.AdvancementTitle" }}</div>
        <div class="item-controls">{{ localize "DEGRINGO5E.Add" }}</div>
    </div>
    <ol class="items-list unlist">
        {{#each advancements}}
        <li class="advancement-item item flexrow">
            <div class="item-name flexrow">
                {{#if svg}}
                <degringo5e-icon src="{{ icon }}"></degringo5e-icon>
                {{else}}
                <img class="gold-icon" src="{{ icon }}" alt="{{ title }}">
                {{/if}}
                <span class="name">{{ title }}</span>
            </div>
            <div class="item-controls flexrow">
                <degringo5e-checkbox name="{{ id }}" checked></degringo5e-checkbox>
            </div>
            {{#if summary}}
            <div class="item-summary">
                {{{ summary }}}
            </div>
            {{/if}}
        </li>
        {{/each}}
    </ol>
</fieldset>
