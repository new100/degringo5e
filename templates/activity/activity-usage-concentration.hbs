<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "DEGRINGO5E.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "degringo5e.activity-usage-notes" }}
    {{/if~}}
</section>
